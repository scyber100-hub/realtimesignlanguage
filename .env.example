# Copy to .env and edit values as needed
API_KEY=
CORS_ALLOW_ORIGINS=*
DEFAULT_START_MS=0
DEFAULT_GAP_MS=60
LOG_LEVEL=INFO
ENABLE_METRICS=1

# Aux channels
INCLUDE_AUX_CHANNELS=1

# Ingest limits
MAX_INGEST_RPS=20
SESSION_TTL_S=600

# Replace window safety (ms)
MAX_REPLACE_FUTURE_MS=1200

# Dashboard thresholds
LATENCY_P90_WARN_MS=1200
REPLACE_RATIO_WARN=0.5
RATE_LIMIT_RATIO_WARN=0.1

