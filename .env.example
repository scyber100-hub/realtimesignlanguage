# Copy to .env and edit values as needed
API_KEY=
CORS_ALLOW_ORIGINS=*
DEFAULT_START_MS=0
DEFAULT_GAP_MS=60
LOG_LEVEL=INFO
ENABLE_METRICS=1

# Optional lexicon
LEXICON_PATH=lexicon/ko_domain_lexicon.json

# Alerts thresholds
LATENCY_P90_WARN_MS=1200
REPLACE_RATIO_WARN=0.5
RATE_LIMIT_RATIO_WARN=0.1
# Alerts history
ALERTS_MAX_ITEMS=500

# Replace quality tuning
REPLACE_MIN_EVENTS=2
REPLACE_MIN_MS=300
REPLACE_MIN_INTERVAL_MS=150

